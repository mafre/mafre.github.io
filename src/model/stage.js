let height = 0;
let width = 0;
let scale = 8;
let mouseX = 0;
let mouseY = 0;
let mouseDown = false;
let windowWidth = 0;
let windowHeight = 0;

export default {
    windowWidth,
    windowHeight,
    height,
    width,
    scale,
    mouseX,
    mouseY,
    mouseDown
}