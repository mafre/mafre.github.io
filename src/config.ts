// Centralized configuration for shared UI/time properties
export const DEFAULT_OPACITY = 0.6;
export const DEFAULT_MARGIN = 16; // used in radius calculation (size/2 - margin)
export const DEFAULT_SVG_WIDTH = 150;
export const DEFAULT_SVG_HEIGHT = DEFAULT_SVG_WIDTH; // keep square for clocks
export const DEFAULT_RADIUS = (DEFAULT_SVG_WIDTH / 2) - DEFAULT_MARGIN;